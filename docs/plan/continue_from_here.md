 # Continue From Here
 
 ## Setup and Run
 - Commands:
   - `npm install`
   - `npm run test:unit`
   - `npm run dev`
 
 ## Next Tasks (choose one and tell Droid to proceed)
 - "Write tests then implement L-Systems engine"
 - "Add IndexedDB history adapter (20-cap) with tests"
 - "Set up Supabase locally (run db/migrations/0001_init.sql) and add RLS tests"
 - "Add export pipeline tests (mp3/wav/ogg/flac)"
 
 ## Saving Progress
 - Commit locally when a chunk is done:
   - `git add -A`
   - `git commit -m "<summary of work>"`
 
 ## Example Prompts
 - "Proceed with L-Systems engine via TDD."
 - "Implement IndexedDB history storage with a 20-item cap using tests first."
 - "Apply the initial Supabase migration and add pgTAP-style policy tests."
 - "Add tests for filename generation and audio encoders, then implement."
